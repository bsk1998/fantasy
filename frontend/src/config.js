// ─── URL du backend FastAPI ────────────────────────────────────────────────
// En développement local : http://localhost:8000
// En production (Render.com) : remplace par l'URL de ton service
export const API_BASE = import.meta.env.VITE_API_BASE || "http://localhost:8000";
